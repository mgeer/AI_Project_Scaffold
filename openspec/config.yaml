schema: spec-driven

# 项目规范见 CLAUDE.md（六大约束：模块化、安全、测试、环境隔离、代码质量、可观测性）

rules:
  proposal:
    - 必须包含安全影响评估（涉及哪些安全约束，如何应对）
    - 必须说明变更涉及的 DDD 层归属
  design:
    - 必须说明各模块的 DDD 层归属及层间接口定义
    - 必须说明测试策略（哪些用单测、哪些用契约测试、哪些用 E2E）
  tasks:
    - 每个任务必须可独立验证，粒度不超过半天工作量
    - 涉及外部依赖的任务必须包含 mock/stub 策略
